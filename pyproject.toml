[project]
name = "wifi"
version = "0.1.0"
requires-python = ">=3.14"

dependencies = [
    "pandas>=3.0.1",
    "rich>=14.3.3",
]

[dependency-groups]
dev = [
    "pandas-stubs>=3.0.0.260204",
]

[tool.basedpyright]
typeCheckingMode = "recommended"
