[project]
name = "wifi"
version = "0.1.1"
readme = "README.md"
requires-python = ">=3.14"

dependencies = [
  "pandas>=3.0.3",
  "prompt-toolkit>=3.0.52",
  "rich>=15.0.0",
]

[dependency-groups]
dev = [
  "pandas-stubs>=3.0.3.260530",
]

[tool.basedpyright]
typeCheckingMode = "recommended"
